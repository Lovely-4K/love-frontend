import { colors } from '~/theme';

const scheduleColors = {
  TRAVEL: colors.base.blue,
  DATE: colors.base.secondary,
  ANNIVERSARY: colors.base.primary,
  ETC: colors.grey[400],
};

export default scheduleColors;
